# Ralph Progress Log
Started: Wed Apr  1 13:31:06 PDT 2026
---

## Codebase Patterns
- Survey management authorization should preserve the existing OWNER bypass and use `hasMembershipPermissionService` with `PERMISSIONS.SURVEYS_MANAGE` for non-owner members.
- The mandatory Ralph quality gate is blocked by Biome errors, but Biome warnings can remain without failing `fix:biome` or `lint:biome`.
- Survey question and option ordering should be normalized from UI array order and persisted as base-1 positions (`index + 1`) to stay aligned with the database constraints.
- Survey create flows that persist parent and child survey records together should use a single database RPC so the use-case stays orchestration-only and the write is truly atomic.
- Public survey submission must treat unauthenticated and anonymous as separate concerns: fingerprint dedupe applies to both, but `is_anonymous` should only be `true` when the respondent explicitly chooses anonymity.
- Cross-table survey integrity that cannot be expressed with a plain foreign key should be enforced with a dedicated Postgres trigger, while letting existing FKs keep ownership of missing-parent errors.
- Survey read RLS should anchor child-table visibility (`survey_questions`, `survey_question_options`) to the parent survey chain instead of duplicating availability and membership predicates in every table policy.
- When enabling RLS on write-only survey flows, prefer `INSERT` operations without `.select()` and generate IDs in the application layer so raw response tables do not need extra `SELECT` policies just to return inserted rows.
- Safe survey response views should be the single place that masks both `respondent_user_id` and manual respondent snapshot fields for anonymous submissions, while preserving identified public snapshots for authorized result consumers.
- Survey module code should prefer aliases from `src/modules/surveys/shared/types/db.ts` over inline `TablesInsert<T>` or enum lookups so schema drift is absorbed in one place.
- Survey server actions should map Supabase rows into camelCase DTOs from `src/modules/surveys/shared/types/dto.ts` so UI-facing contracts stay stable even when database columns remain snake_case.
- Public survey listing should apply publication plus active date-window filtering in the backend and expose a narrowed DTO so `/surveys` does not need extra availability filtering or raw survey metadata.
- Dashboard survey listing should gate on active membership and expose a dedicated DTO mapper instead of reusing the broader survey summary contract.
- Dashboard survey detail loaders for member response pages should authenticate first, resolve the tenant by host, require active membership, and then rely on tenant-scoped RLS reads instead of admin bypasses so only answerable surveys stay visible.
- Once a survey has responses, update flows must not reuse the delete-and-reinsert question sync path; compare the incoming structure against persisted questions/options and reject structural changes before touching historical rows.
- Survey status transitions should be enforced in the use-case layer with explicit `invalid_publication_state` responses, allowing only `draft -> published` and `published -> closed`.
- Public survey detail loaders that must distinguish `draft`, `closed`, `unavailable`, and `cross-tenant` states should inspect survey metadata through an admin repo first, then load public questions only after the tenant and availability checks pass.
- Survey response submission should resolve the current tenant from the request host when possible, fall back to an explicit `organizationId` only when headers are unavailable, and validate the survey through an admin lookup before relying on RLS-backed question reads.
- Response flows that must persist a parent `survey_responses` row plus child `survey_response_items` rows should use one database RPC and keep `already_answered` mapped from the unique constraint, so dedupe and atomicity stay aligned.
- Survey results aggregation should keep authorization in the use-case and use an admin repo to load question structure plus response items in one read, leaving percentage and ranking math to the service layer.
- Qualitative survey results should source respondent metadata from `survey_response_items_safe`, then map that safe projection into DTOs instead of exposing raw response rows or rebuilding anonymity masking in TypeScript.
- Public survey detail pages that must show closed or not-yet-open states should validate tenant and visibility via admin survey metadata first, then use an admin question loader so the shell can still render the survey structure even when public read RLS blocks out-of-window questions.
- Public response UX should extend `SurveyRenderer` through pre-submit composition hooks instead of duplicating question rendering, so identity controls and answer validation stay in one shared flow.
- Dashboard survey index pages can reuse `getSidebarContextAction` for UI-only permission gating while keeping the actual survey list read on `list-dashboard-surveys`, which already owns tenant and membership enforcement.
- Shared survey management forms should keep `startsAt` and `endsAt` in ISO strings in component state and convert to `datetime-local` only at the input boundary, so builder consumers stay aligned with the action schemas.
- Dashboard survey management pages that must allow OWNER access should use a dedicated server context slice that resolves the tenant and checks membership role plus `surveys.manage`, instead of relying only on sidebar permission keys.

---

## 2026-04-02 16:44 UTC - US-027
- Added the dashboard survey edit/manage route and a dedicated management context slice that resolves tenant, organization, survey detail, and structural lock state for authorized managers.
- Built a client management screen that reuses `SurveyBuilder` for editing, keeps metadata editable, disables structural changes when responses already exist, and wires save, publish, and close actions with status-aware controls.
- Files changed: `src/app/dashboard/surveys/[surveyId]/edit/page.tsx`, `src/modules/surveys/server/slices/get-manage-survey-context/actions/get-manage-survey-context.action.ts`, `src/modules/surveys/server/slices/get-manage-survey-context/use-cases/get-manage-survey-context.use-case.ts`, `src/modules/surveys/shared/ui/manage-survey-form.tsx`, `src/modules/surveys/shared/validations/get-manage-survey-context.schema.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Browser verification using the dev-browser skill was not possible because that skill/tooling is unavailable in this Codex session, so manual browser verification is still required.
- **Learnings for future iterations:**
  - Dashboard management surfaces should load permission, organization metadata, survey detail, and response-lock state in one server context slice instead of duplicating these checks between the route and client form.
  - `SurveyBuilder` already supports the correct lock semantics for post-response surveys, so edit flows should reuse it and let the backend remain the source of truth for `survey_locked_after_response`.
  - Status-aware management CTAs are simplest when the client tracks only the mutable survey status locally and leaves transition validation to the existing publish and close actions.
---

## 2026-04-02 16:34 UTC - US-022
- Updated the public survey detail shell so it now reuses `SurveyRenderer` to render all four question types and shows availability messaging alongside `startsAt` and `endsAt`.
- Extended the public detail DTO with an `accessState` field and changed the public detail use-case to keep closed and out-of-window public surveys renderable instead of returning them as hard errors.
- Added an admin-backed survey question loader for the public detail route because the current RLS policy hides question rows when a public survey is outside its active response window.
- Files changed: `src/modules/surveys/server/repos/list-survey-questions-with-options.admin.repo.ts`, `src/modules/surveys/server/services/list-survey-questions-with-options-admin.service.ts`, `src/modules/surveys/server/slices/load-public-survey-detail/use-cases/load-public-survey-detail.use-case.ts`, `src/modules/surveys/shared/types/dto.ts`, `src/modules/surveys/shared/ui/public-survey-detail-view.tsx`, `src/modules/surveys/shared/ui/survey-renderer.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Browser verification using the dev-browser skill was not possible because that skill/tooling is unavailable in this Codex session, so manual browser verification is still required.
- **Learnings for future iterations:**
  - Public detail shells should carry an explicit `accessState` in the DTO so UI flows can distinguish renderable blocked states from true not-found or cross-tenant errors without recomputing availability rules.
  - The current survey read RLS is intentionally stricter than the public shell requirement, so closed and future-window survey detail pages need a controlled admin question read after tenant and visibility checks.
  - `SurveyRenderer` is now the shared contract for all public question-type rendering, and future public submission stories should extend it instead of creating a second renderer path.
---

## 2026-04-02 16:21 UTC - US-023
- Added a dedicated public response client form that lets unauthenticated respondents choose between anonymous and identified submission modes before sending answers.
- Wired the public survey detail page to the existing `submitSurveyResponseAction`, requiring name, email, and phone for identified public submissions and hiding those fields when anonymous mode is selected.
- Extended `SurveyRenderer` with pre-submit composition hooks so identity UX can be injected without duplicating the shared question rendering and validation flow.
- Files changed: `src/modules/surveys/shared/ui/public-survey-response-form.tsx`, `src/modules/surveys/shared/ui/public-survey-detail-view.tsx`, `src/modules/surveys/shared/ui/survey-renderer.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Browser verification using the dev-browser skill was not possible because that skill/tooling is unavailable in this Codex session, so manual browser verification is still required.
- **Learnings for future iterations:**
  - Public response forms should keep respondent identity state outside the raw question answer payload, then merge both only at submit time when calling the server action.
  - When a public survey allows anonymity, the UI should default to the anonymous mode and reveal identity fields only when the respondent explicitly opts into identified submission.
  - Extending `SurveyRenderer` with reusable pre-submit hooks keeps the shared answer validation intact while allowing surface-specific UX such as identity collection.
---

## 2026-04-02 16:08 UTC - US-020
- Extended the survey results DTO contract so `textarea` questions now return structured qualitative answers with `responseId`, `answerText`, `submittedAt`, `isAnonymous`, and a nested respondent identity snapshot.
- Updated the admin results read and aggregation service to include `textarea` questions and pull respondent metadata from `survey_response_items_safe`, keeping anonymous identities masked by the database view.
- Files changed: `src/modules/surveys/shared/types/dto.ts`, `src/modules/surveys/server/repos/list-survey-results.admin.repo.ts`, `src/modules/surveys/server/services/load-survey-results.service.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Manual browser verification was not run because this story changes backend results loading only.
- **Learnings for future iterations:**
  - Qualitative result payloads should expose a dedicated DTO per text answer rather than overloading the aggregate option contract, because review UIs need submission metadata and anonymity state per response.
  - When anonymity masking already exists in SQL safe views, services should preserve that contract instead of re-implementing conditional field stripping in application code.
  - Sorting text answers by `submittedAt` in the service keeps downstream dashboard pages simple while preserving the DTO boundary from raw database rows.
---

## 2026-04-02 16:03 UTC - US-019
- Added a `load-survey-results` backend slice with Zod validation and a manager-only use-case that authorizes `OWNER` or `surveys.manage`, validates the organization, and rejects surveys outside the tenant.
- Added an admin-backed aggregation service that loads choice and ranking questions with their options and response items, then returns per-option counts, percentages, and average rank metrics in `SurveyResultsDTO`.
- Added an admin response-count service so the results contract exposes the survey-level total responses without relying on RLS reads from raw response tables.
- Files changed: `src/modules/surveys/server/repos/list-survey-results.admin.repo.ts`, `src/modules/surveys/server/repos/count-survey-responses.admin.repo.ts`, `src/modules/surveys/server/services/load-survey-results.service.ts`, `src/modules/surveys/server/services/count-survey-responses-admin.service.ts`, `src/modules/surveys/shared/validations/load-survey-results.schema.ts`, `src/modules/surveys/server/slices/load-survey-results/actions/load-survey-results.action.ts`, `src/modules/surveys/server/slices/load-survey-results/use-cases/load-survey-results.use-case.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Manual browser verification was not run because this story adds backend-only results loading.
- **Learnings for future iterations:**
  - Results backends should aggregate from one admin read over question structure plus response items, which avoids multi-repo counting logic inside services while keeping manager auth outside the data layer.
  - Ranking questions benefit from an explicit `averageRank` metric per option, while keeping `responseCount` and `percentage` aligned with the count of respondents that placed each option anywhere in their ranking.
  - A separate admin response-count read keeps the top-level survey total accurate even when future result slices only project a subset of question types.
---

## 2026-04-02 15:59 UTC - US-018
- Replaced the two-step response write path with a single `submit_survey_response` RPC that inserts the response header and all answer items in one transaction.
- Kept dedupe on the existing database unique constraints, so authenticated submissions still collide on `(survey_id, respondent_user_id)` and anonymous or public identified submissions still collide on `(survey_id, responder_fingerprint_hash)` with the same `already_answered` contract.
- Preserved the public respondent snapshot contract by sending manual identity fields only for unauthenticated identified public submissions, while anonymous submissions continue to persist those fields as `null`.
- Files changed: `supabase/migrations/20260402162000_submit_survey_response_rpc.sql`, `src/modules/surveys/server/repos/submit-survey-response.repo.ts`, `src/modules/surveys/server/services/submit-survey-response.service.ts`, `src/modules/surveys/server/slices/submit-survey-response/use-cases/submit-survey-response.use-case.ts`, `src/shared/types/supabase.d.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Manual browser verification was not run because this story changes backend persistence only.
- **Learnings for future iterations:**
  - Response writes that span multiple survey tables should move the transaction boundary into Postgres, because app-level sequencing cannot recover cleanly from child insert failures after the response header already exists.
  - Keeping dedupe ownership in the database is preferable to pre-check queries, since the unique indexes already model the authenticated vs fingerprint split and remain race-safe under concurrent submissions.
  - Returning the inserted `survey_responses` row from the RPC lets the action keep the existing DTO contract without re-querying raw response tables under RLS.
---

## 2026-04-02 15:50 UTC - US-016
- Added a dashboard survey detail loader slice that authenticates the current user, resolves the current tenant from the request host, enforces active membership, and returns an ordered `SurveyDetailDTO`.
- Kept the member detail read on regular tenant-scoped survey/question services so public and private surveys remain constrained by the existing dashboard RLS contract instead of using an admin bypass.
- Files changed: `src/modules/surveys/shared/validations/load-dashboard-survey-detail.schema.ts`, `src/modules/surveys/server/slices/load-dashboard-survey-detail/actions/load-dashboard-survey-detail.action.ts`, `src/modules/surveys/server/slices/load-dashboard-survey-detail/use-cases/load-dashboard-survey-detail.use-case.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story adds a backend loader slice but no browser-rendered route yet.
- **Learnings for future iterations:**
  - Dashboard member loaders should reuse the same auth and tenant resolution steps as the dashboard guard, but still keep their own explicit `not_member` and `survey_not_found` contract for page-level handling.
  - For answer flows, loading the survey through regular RLS-backed repos is preferable to admin reads because unpublished or unavailable surveys should naturally collapse to "not readable" for non-manager members.
- Reusing `SurveyDetailDTO` avoids introducing a second member-detail contract when the dashboard page needs full ordered questions plus tenant-safe survey metadata.
---

## 2026-04-02 15:55 UTC - US-017
- Hardened `submit-survey-response` so it now resolves the current tenant, loads survey metadata before persistence, rejects cross-tenant or unavailable surveys, enforces active membership for private surveys, and blocks anonymous submissions when the survey forbids them.
- Required `respondentName`, `respondentEmail`, and `respondentPhone` for unauthenticated identified public responses, and persisted the response under the survey organization instead of trusting caller-provided organization data.
- Tightened answer validation to reject any `questionId` that does not belong to the loaded survey before inserts begin.
- Files changed: `src/modules/surveys/server/slices/submit-survey-response/actions/submit-survey-response.action.ts`, `src/modules/surveys/server/slices/submit-survey-response/use-cases/submit-survey-response.use-case.ts`, `src/modules/surveys/shared/validations/survey-response.schema.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Manual browser verification was not run because this story changes backend submission validation only.
- **Learnings for future iterations:**
  - Response submission should classify access from survey metadata first, because RLS-backed question reads alone cannot distinguish cross-tenant, unpublished, or out-of-window surveys cleanly.
  - Extra answer entries must be rejected explicitly; iterating only over known questions is not enough because unknown `questionId`s otherwise bypass validation.
  - Response rows should derive `organization_id` from the loaded survey, not from caller input, so tenant integrity is preserved even when the client payload is tampered with.
---

## 2026-04-02 15:45 UTC - US-015
- Added a public survey detail loader slice that resolves the tenant from the current host, classifies cross-tenant and availability errors explicitly, and returns a narrowed public detail DTO with ordered questions and options.
- Introduced an admin-backed survey lookup plus a reusable public-access-state helper so the loader can distinguish `draft`, `closed`, and other unavailable states even though public RLS only exposes already-answerable surveys.
- Added the `/surveys/[surveyId]` server route and module-scoped presentation UI to render the survey detail contract or a deterministic business-error state without moving domain logic into `src/app`.
- Files changed: `src/modules/surveys/shared/types/dto.ts`, `src/modules/surveys/server/repos/find-survey-by-id.admin.repo.ts`, `src/modules/surveys/server/services/find-survey-by-id-admin.service.ts`, `src/modules/surveys/shared/utils/get-public-survey-access-state.ts`, `src/modules/surveys/shared/validations/load-public-survey-detail.schema.ts`, `src/modules/surveys/server/slices/load-public-survey-detail/actions/load-public-survey-detail.action.ts`, `src/modules/surveys/server/slices/load-public-survey-detail/use-cases/load-public-survey-detail.use-case.ts`, `src/modules/surveys/shared/ui/public-survey-detail-view.tsx`, `src/app/surveys/[surveyId]/page.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification is still required because this story adds a new public page contract and browser tooling was unavailable in this environment.
- **Learnings for future iterations:**
  - Public detail loaders cannot rely on regular public-table reads when the product contract needs to distinguish hidden-but-existing states, because RLS intentionally collapses those rows to “not readable”.
  - A narrow `SurveyPublicDetailDTO` keeps the page contract focused on answerable metadata instead of leaking management-only fields such as `organizationId` or `createdByUserId`.
  - Keeping the route file thin and pushing survey-specific rendering into `src/modules/surveys/shared/ui/` preserves the App Router boundary from `AGENTS.md` while still letting the page render meaningful fallback states.
---

## 2026-04-02 15:39 UTC - US-014
- Finalized survey status transition rules by rejecting invalid publish and close operations with the explicit business code `invalid_publication_state`.
- Kept publish constrained to draft surveys with at least one question, and close constrained to published surveys so the existing response availability rules continue to treat closed surveys as unavailable.
- Files changed: `src/modules/surveys/server/slices/publish-survey/use-cases/publish-survey.use-case.ts`, `src/modules/surveys/server/slices/publish-survey/actions/publish-survey.action.ts`, `src/modules/surveys/server/slices/close-survey/use-cases/close-survey.use-case.ts`, `src/modules/surveys/server/slices/close-survey/actions/close-survey.action.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story changes backend mutation rules rather than browser-rendered UI.
- **Learnings for future iterations:**
  - Survey publish/close actions should validate the current `status` before calling the write service; otherwise the repo layer will happily apply no-op or invalid transitions without a stable business error.
  - Reusing one `invalid_publication_state` code across publish and close keeps the action contract explicit without leaking raw status strings to the UI.
  - Closing a survey does not need extra response-write code when the submit path already relies on “published and currently available” backend rules; the critical requirement is to make the close transition itself unambiguous.
---

## 2026-04-02 15:31 UTC - US-012
- Implemented an atomic survey creation RPC that inserts the survey, its questions, and their options inside one database transaction while normalizing positions from payload order to base-1.
- Replaced the multi-step `create-survey` persistence flow with a single service/repo RPC call and added pre-persistence date-range validation in the action schema.
- Files changed: `supabase/migrations/20260402110000_create_survey_with_questions_rpc.sql`, `src/modules/surveys/server/repos/create-survey-with-questions.repo.ts`, `src/modules/surveys/server/services/create-survey-with-questions.service.ts`, `src/modules/surveys/server/slices/create-survey/use-cases/create-survey.use-case.ts`, `src/modules/surveys/shared/validations/create-survey.schema.ts`, `src/shared/types/supabase.d.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification is still required because browser tooling was unavailable and this story affects the create-survey workflow contract.
- **Learnings for future iterations:**
  - The existing survey create flow was only pseudo-atomic; if child inserts can fail, the module should prefer one Postgres function over app-layer rollback attempts.
  - When adding RPCs manually, `src/shared/types/supabase.d.ts` must reflect nullable arguments exactly or the repo layer will fail typecheck even when the SQL signature is correct.
  - Base-1 ordering is safest when derived from `jsonb_array_elements` iteration order inside the RPC instead of trusting client-provided `position` fields.
---

## 2026-04-01 21:18 UTC - US-004
- Implemented a database integrity trigger for `public.survey_response_items` so each item now verifies that its `question_id` belongs to the same survey as the parent `response_id`.
- Added a dedicated Supabase migration that raises a check-style database error before commit when a tampered payload tries to mix questions from another survey.
- Files changed: `supabase/migrations/20260401230000_enforce_survey_response_item_survey_integrity.sql`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story is database/backend-only.
- **Learnings for future iterations:**
  - `survey_response_items` needs trigger-based cross-table validation because the invariant depends on both `response_id` and `question_id`, not just one foreign key.
  - The safest pattern here is to return early when parent rows are missing and let the existing foreign keys emit their standard database errors.
  - A `23514` check-style exception is a good fit for rejecting tampered cross-survey payloads without changing the table shape.
---

## 2026-04-01 20:59 UTC - US-001
- Implemented the dedicated `surveys.manage` permission contract across auth shared constants and survey management use-cases.
- Added a Supabase migration to seed `surveys.manage` in `public.permissions`.
- Files changed: `src/modules/auth/shared/permissions.ts`, `src/modules/auth/shared/permission-presenter.ts`, `src/modules/surveys/server/slices/create-survey/use-cases/create-survey.use-case.ts`, `src/modules/surveys/server/slices/close-survey/use-cases/close-survey.use-case.ts`, `src/modules/surveys/server/slices/publish-survey/use-cases/publish-survey.use-case.ts`, `src/modules/surveys/server/slices/update-survey/use-cases/update-survey.use-case.ts`, `supabase/migrations/20260401143000_seed_permission_surveys_manage.sql`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: `typecheck` passed, but the full gate is still blocked by pre-existing Biome errors outside the surveys scope (`src/app/lp/page.tsx`, `src/shared/components/vortex.tsx`, `src/modules/organizations/insights/people-map/shared/ui/world-people-map-maplibre.tsx`), so this story was not marked as passed and no commit was created.
- **Learnings for future iterations:**
  - Survey authorization is already centralized in use-cases, so permission-key changes should be applied there rather than in repos or services.
  - The repository baseline currently has global Biome violations outside surveys; future Ralph iterations should expect the mandatory quality gate to fail until that baseline is cleaned up.
  - Auth permission labels are maintained in `permission-presenter.ts`; new permission keys need both the constant and the friendly label to keep admin surfaces readable.
---

## 2026-04-01 21:01 UTC - US-001
- Re-validated that the highest-priority pending story is still `US-001` on branch `ralph/surveys-module`, and confirmed the permission/migration implementation is already present in the worktree.
- Re-ran the required quality gates for this story: `npm run fix:biome`, `npm run lint:biome`, and `npm run typecheck`.
- Files changed: `scripts/ralph/progress.txt`.
- Result: `typecheck` passed again, but `fix:biome` and `lint:biome` still fail on unrelated baseline files (`src/app/lp/page.tsx`, `src/shared/components/ui/calendar.tsx`, `src/shared/components/ui/field.tsx`, `src/shared/components/ui/input-group.tsx`, `src/shared/components/vortex.tsx`, `src/modules/organizations/insights/people-map/shared/ui/world-people-map-maplibre.tsx`), so `US-001` remains uncommitted and `passes` stays `false`.
- **Learnings for future iterations:**
  - Ralph should treat `US-001` as functionally implemented but blocked by repository-wide Biome debt until those unrelated files are cleaned up.
  - Running `npm run fix:biome` does not clear the current blockers because several diagnostics require manual refactors, not safe auto-fixes.
  - With a dirty shared worktree, unrelated modified files should not be folded into the story commit just to satisfy the gate.
---

## 2026-04-01 21:02 UTC - US-001
- Confirmed branch `ralph/surveys-module` matches `scripts/ralph/prd.json` and that `US-001` is still the highest-priority story with `passes: false`.
- Re-validated the existing `surveys.manage` implementation across auth shared permission files, survey management use-cases, and the Supabase seed migration.
- Files changed: `scripts/ralph/progress.txt`.
- Result: `npm run typecheck` passed, but `npm run fix:biome` and `npm run lint:biome` still fail on unrelated baseline files (`src/app/lp/page.tsx`, `src/shared/components/ui/calendar.tsx`, `src/shared/components/ui/input-group.tsx`, `src/shared/components/vortex.tsx`, `src/modules/organizations/insights/people-map/shared/ui/world-people-map-maplibre.tsx`), so this iteration cannot mark `US-001` as passed or create the required commit.
- **Learnings for future iterations:**
  - `US-001` does not need additional product code; the remaining blocker is the repository-wide Biome baseline outside the story scope.
  - The mandatory Ralph loop should keep `passes: false` when the implementation exists but the required quality gate cannot pass cleanly.
  - Future iterations should avoid touching unrelated lint-debt files unless the selected story explicitly includes that cleanup work.
---

## 2026-04-01 21:08 UTC - US-001
- Finalized the `surveys.manage` permission story by keeping the dedicated auth constant, friendly label, seed migration, and survey management use-cases aligned on the new permission key.
- Unblocked the mandatory repository quality gate by fixing the current Biome error set in shared UI and visualization files without changing the survey contract itself.
- Files changed: `src/modules/auth/shared/permissions.ts`, `src/modules/auth/shared/permission-presenter.ts`, `src/modules/surveys/server/slices/create-survey/use-cases/create-survey.use-case.ts`, `src/modules/surveys/server/slices/close-survey/use-cases/close-survey.use-case.ts`, `src/modules/surveys/server/slices/publish-survey/use-cases/publish-survey.use-case.ts`, `src/modules/surveys/server/slices/update-survey/use-cases/update-survey.use-case.ts`, `supabase/migrations/20260401143000_seed_permission_surveys_manage.sql`, `src/app/lp/page.tsx`, `src/shared/components/vortex.tsx`, `src/shared/components/ui/calendar.tsx`, `src/shared/components/ui/field.tsx`, `src/shared/components/ui/input-group.tsx`, `src/shared/components/ui/sidebar.tsx`, `src/modules/organizations/insights/people-map/shared/data/brazilian-city-coordinates.ts`, `src/modules/organizations/insights/people-map/shared/ui/world-people-map-maplibre.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story is backend/permission-focused.
- **Learnings for future iterations:**
  - Shared UI lint debt can block unrelated backend stories because Ralph's gate runs against the whole `src/` tree.
  - The current shared component baseline tolerates the existing `document.cookie` warning in `sidebar.tsx`; it does not block Biome checks.
  - The survey permission change itself remains localized to auth shared constants, the seed migration, and survey management use-cases.
---

## 2026-04-01 21:19 UTC - US-002
- Implemented the base-1 ordering contract for surveys by correcting persisted question and option positions in both create and update flows.
- Added a corrective Supabase migration that reindexes legacy `survey_questions` and `survey_question_options` rows to base 1 using deterministic window ordering.
- Updated the shared survey builder and Zod validation schema so client payloads now produce and accept only base-1 positions.
- Files changed: `supabase/migrations/20260401214500_realign_survey_positions_to_base_1.sql`, `src/modules/surveys/shared/ui/survey-builder.tsx`, `src/modules/surveys/shared/validations/survey-question.schema.ts`, `src/modules/surveys/server/slices/create-survey/use-cases/create-survey.use-case.ts`, `src/modules/surveys/server/slices/update-survey/use-cases/update-survey.use-case.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Browser verification was not run because this story only changed ordering contract and persistence logic.
- **Learnings for future iterations:**
  - The surveys module currently derives structural order from array order, so server persistence should continue to normalize positions instead of trusting incoming `position` values blindly.
  - Reindex migrations that touch unique `(parent_id, position)` constraints are safer when they use `row_number()` over a deterministic ordering instead of incremental updates.
  - UI helpers that remove or reorder nested survey entities should renormalize both question and option positions together to avoid stale payload state.
---

## 2026-04-01 21:16 UTC - US-003
- Added a Supabase migration to create nullable `respondent_name`, `respondent_email`, and `respondent_phone` on `public.survey_responses`, and updated the anonymity trigger so anonymous submissions always clear manual identity while identified submissions can use either `respondent_user_id` or the manual snapshot.
- Extended the survey response action schema and use-case so unauthenticated identified submissions can persist manual respondent fields without being forced into `is_anonymous = true`, while fingerprint dedupe still applies.
- Updated `src/shared/types/supabase.d.ts` to include the new `survey_responses` fields because the repository's `npm run db:gen-types` command currently requires Supabase CLI auth that is unavailable in this Codex environment.
- Files changed: `supabase/migrations/20260401223000_add_public_respondent_identity_fields.sql`, `src/modules/surveys/shared/validations/submit-survey-response.schema.ts`, `src/modules/surveys/server/slices/submit-survey-response/actions/submit-survey-response.action.ts`, `src/modules/surveys/server/slices/submit-survey-response/use-cases/submit-survey-response.use-case.ts`, `src/shared/types/supabase.d.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run db:gen-types` (failed: missing Supabase access token after truncating the target file, which was restored), `npm run fix:biome`, `npm run lint:biome`, and `npm run typecheck`.
- Result: the required quality gates passed. Manual browser verification was not run because this story is schema/backend-only.
- **Learnings for future iterations:**
- The current `db:gen-types` script redirects before Supabase CLI authentication completes, so a failed auth attempt can empty `src/shared/types/supabase.d.ts`; restore the file before applying manual follow-up changes.
- Manual respondent identity belongs in the submission contract, not only in the database schema, otherwise the current submit flow will keep collapsing public identified responses into anonymous ones.
- The existing anonymity trigger is the right place to enforce nulling identity fields for anonymous responses so future services and views can rely on sanitized rows.
---

## 2026-04-01 21:20 UTC - US-005
- Enabled RLS read policies for `public.surveys`, `public.survey_questions`, and `public.survey_question_options`.
- Added reusable SQL helpers so survey reads now distinguish public published-and-available access, private access for active members, and full tenant management access for `OWNER` or `surveys.manage`.
- Files changed: `supabase/migrations/20260401234500_enable_survey_read_rls.sql`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story is database/backend-only.
- **Learnings for future iterations:**
  - Child survey read policies are simpler and more durable when they defer to the readable parent row instead of repeating tenant, availability, and permission predicates.
  - Keeping survey management access in a dedicated SQL helper makes future response and safe-view policies easier to align with the `OWNER` plus `surveys.manage` contract.
  - The repository-wide Biome warning in `src/shared/components/ui/sidebar.tsx` remains non-blocking for Ralph's mandatory gate because it is emitted as a warning, not an error.
---

## 2026-04-01 21:25 UTC - US-006
- Enabled RLS write policies for `public.survey_responses` and `public.survey_response_items`.
- Added security-definer SQL helpers so response inserts now require a published, currently available survey, while private survey submissions also require active membership.
- Removed `insert(...).select(...)` from the submission repos and generated response IDs in the use-case so the write path keeps working under RLS without exposing raw response-table reads.
- Files changed: `supabase/migrations/20260402001000_enable_survey_response_write_rls.sql`, `src/modules/surveys/server/repos/insert-survey-response.repo.ts`, `src/modules/surveys/server/repos/insert-survey-response-items.repo.ts`, `src/modules/surveys/server/services/submit-survey-response.service.ts`, `src/modules/surveys/server/slices/submit-survey-response/use-cases/submit-survey-response.use-case.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story is database/backend-only.
- **Learnings for future iterations:**
  - Write RLS for response child tables is easier to keep consistent when the policy delegates survey availability and membership checks to security-definer helper functions.
  - Returning inserted rows from raw response tables under RLS forces extra read policies, so generating IDs client-side and skipping `.select()` is the safer default for submit-only flows.
  - Response-item insert policies should validate against both the parent response and the target question so private-survey restrictions stay aligned even after the response header is created.
---

## 2026-04-01 21:27 UTC - US-007
- Updated the safe survey response views so anonymous responses now null out both `respondent_user_id` and the manual respondent snapshot fields, while identified public responses keep `respondent_name`, `respondent_email`, and `respondent_phone` available through the safe projection.
- Added Supabase type coverage and survey DB aliases for `survey_responses_safe` and `survey_response_items_safe` so the codebase can consume the updated views without raw table access.
- Files changed: `supabase/migrations/20260402002000_update_survey_safe_response_views.sql`, `src/shared/types/supabase.d.ts`, `src/modules/surveys/shared/types/db.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story is database/backend-only.
- **Learnings for future iterations:**
  - When survey result consumers need respondent identity, expose it via safe views instead of the raw `survey_responses` table so anonymity rules stay centralized.
  - `survey_response_items_safe` should mirror the identity columns from `survey_responses_safe`; otherwise downstream analytics consumers may accidentally fall back to raw joins.
  - Manual Supabase type maintenance needs the `Views` section updated whenever migrations redefine SQL views, even if table schemas stay unchanged.
---

## 2026-04-01 23:24 UTC - US-008
- Expanded the survey module DB aliases to cover inserts, updates, enums, and safe-view rows, then replaced inline `TablesInsert<...>` usages in survey create/update persistence with those aliases.
- Updated `src/shared/types/supabase.d.ts` so the generated contract includes the survey RLS helper functions introduced by the final schema migrations.
- Files changed: `src/modules/surveys/shared/types/db.ts`, `src/modules/surveys/server/repos/insert-survey-questions.repo.ts`, `src/modules/surveys/server/repos/insert-survey-question-options.repo.ts`, `src/modules/surveys/server/services/insert-survey-questions.service.ts`, `src/modules/surveys/server/services/insert-survey-question-options.service.ts`, `src/modules/surveys/server/slices/create-survey/use-cases/create-survey.use-case.ts`, `src/modules/surveys/server/slices/update-survey/use-cases/update-survey.use-case.ts`, `src/shared/types/supabase.d.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story only changes TypeScript contracts and backend typing.
- **Learnings for future iterations:**
  - Survey schema aliases are easier to keep consistent when all module-facing inserts, updates, enums, and safe-view rows live in one `shared/types/db.ts` file.
  - Manual Supabase type maintenance must include SQL helper functions created by migrations, not only tables and views, otherwise RPC references drift from the actual schema.
  - The connected Supabase project can lag behind local migrations, so Ralph should compare generated output against the local migration set before trusting it as the final contract.
---

## 2026-04-01 23:41 UTC - US-009
- Expanded the shared survey DTO contract with explicit list, summary, detail, public respondent identity, response, and results shapes, plus reusable mappers for survey rows and response rows.
- Updated survey server actions to return camelCase DTOs instead of raw Supabase row types for public list, dashboard list, create, update, publish, close, and submit-response flows.
- Files changed: `src/modules/surveys/shared/types/dto.ts`, `src/modules/surveys/server/slices/list-public-surveys/actions/list-public-surveys.action.ts`, `src/modules/surveys/server/slices/list-dashboard-surveys/actions/list-dashboard-surveys.action.ts`, `src/modules/surveys/server/slices/create-survey/actions/create-survey.action.ts`, `src/modules/surveys/server/slices/update-survey/actions/update-survey.action.ts`, `src/modules/surveys/server/slices/publish-survey/actions/publish-survey.action.ts`, `src/modules/surveys/server/slices/close-survey/actions/close-survey.action.ts`, `src/modules/surveys/server/slices/submit-survey-response/actions/submit-survey-response.action.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification was not run because this story changes shared backend contracts rather than browser-rendered UI.
- **Learnings for future iterations:**
  - DTO mappers belong in the surveys shared contract layer so multiple actions can reuse one camelCase conversion instead of hand-mapping the same row fields repeatedly.
  - Distinguishing `SurveySummaryDTO` from future `SurveyDetailDTO` keeps current mutation/list responses lean while reserving a richer contract for the upcoming detail loaders.
  - Public response DTOs should expose respondent snapshot data through a nested identity object and omit storage-only fields such as fingerprint hashes.
---

## 2026-04-01 23:31 UTC - US-010
- Refactored the public survey listing backend so the service now filters surveys by the active availability window (`starts_at`/`ends_at`) before returning results for the current tenant domain.
- Narrowed `SurveyPublicListItemDTO` to the public listing contract and added a dedicated mapper so the action now returns only `id`, `title`, `description`, `status`, `visibility`, `startsAt`, `endsAt`, and `acceptAnonymousAnswers`.
- Files changed: `src/modules/surveys/shared/types/dto.ts`, `src/modules/surveys/server/services/list-public-surveys.service.ts`, `src/modules/surveys/server/slices/list-public-surveys/use-cases/list-public-surveys.use-case.ts`, `src/modules/surveys/server/slices/list-public-surveys/actions/list-public-surveys.action.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification is still required because this story affects the `/surveys` UI contract and browser tooling was unavailable in this environment.
- **Learnings for future iterations:**
  - Public listing availability should be derived from the current time against `starts_at` and `ends_at`, not left for the UI to infer after fetching mixed survey states.
  - A dedicated public-list DTO avoids leaking dashboard-only metadata such as organization ownership fields into anonymous surfaces.
  - When the repo still fetches full survey rows, the service layer is the right place to strip fields and enforce list-specific business filtering before the action responds.
---

## 2026-04-01 23:34 UTC - US-011
- Refactored the dashboard surveys listing backend to require an authenticated user with active membership in the current tenant before returning survey data.
- Narrowed `SurveyDashboardListItemDTO` to dashboard list metadata only and added a dedicated mapper so the action no longer reuses the broader survey summary contract.
- Files changed: `src/modules/surveys/server/slices/list-dashboard-surveys/actions/list-dashboard-surveys.action.ts`, `src/modules/surveys/server/slices/list-dashboard-surveys/use-cases/list-dashboard-surveys.use-case.ts`, `src/modules/surveys/shared/types/dto.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification is still required because this story affects the dashboard listing contract and browser tooling was unavailable in this environment.
- **Learnings for future iterations:**
  - The dashboard listing should check `isActive` explicitly even when the membership service also derives a convenience `isMember` flag, because the story contract is about active membership.
  - Separate mappers for public and dashboard list surfaces keep list DTOs intentionally narrow without forcing the rest of the survey summary contract onto every consumer.
  - The existing survey repo can still fetch tenant survey rows broadly while the action layer owns the responsibility for shaping the safe UI contract.
---

## 2026-04-02 15:35 UTC - US-013
- Implemented a response-aware lock in `update-survey` so any question or option structure change after the first submission now fails with the explicit business code `survey_locked_after_response`.
- Added a dedicated response-count repo/service and reused the ordered question loader to compare the incoming question contract against the persisted survey structure before any destructive sync runs.
- Preserved metadata-only survey edits after responses by skipping the delete-and-reinsert question sync when the submitted structure matches the stored one exactly.
- Files changed: `src/modules/surveys/server/repos/count-survey-responses.repo.ts`, `src/modules/surveys/server/services/count-survey-responses.service.ts`, `src/modules/surveys/server/slices/update-survey/use-cases/update-survey.use-case.ts`, `src/modules/surveys/server/slices/update-survey/actions/update-survey.action.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification is still required because this story affects the survey edit workflow and browser tooling was unavailable in this environment.
- **Learnings for future iterations:**
  - Survey edit flows that rebuild child records must guard against existing responses before deleting question rows, otherwise historical response items lose their referential meaning even if the top-level survey update succeeds.
  - Comparing normalized persisted question/option content against the incoming payload is safer than keying only on payload presence because the current editor can resend the unchanged structure during metadata-only saves.
  - The explicit `survey_locked_after_response` business code belongs in both the use-case and the action contract so the UI can surface a stable lock-state message without inferring from generic infra failures.
---

## 2026-04-02 16:11 UTC - US-021
- Created the public surveys listing route as a server component in `src/app/surveys/page.tsx`, wired to `listPublicSurveysAction`, and delegated domain rendering to the surveys module UI layer.
- Added a dedicated public listing view with survey cards that show title, shortened description, status, response deadline, anonymity behavior, and a CTA to open the survey response page.
- Added explicit public empty and error states so the route stays predictable when the tenant has no available surveys or the tenant lookup fails.
- Files changed: `src/app/surveys/page.tsx`, `src/modules/surveys/shared/ui/public-surveys-list-view.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Manual browser verification is still required because the requested browser tooling/skill is unavailable in this Codex session.
- **Learnings for future iterations:**
  - Public App Router entrypoints should stay thin and server-first, with tenant-aware loading in the action/use-case layer and survey-specific presentation moved into `src/modules/surveys/shared/ui/`.
  - The public surveys list contract already arrives filtered to currently available surveys, so the page should focus on presentation and empty/error handling instead of re-implementing availability rules.
  - Shortening long survey descriptions in the presentation layer keeps list cards stable without weakening the full-detail contract used by the survey detail page.
---

## 2026-04-02 16:25 UTC - US-024
- Created the `/dashboard/surveys` server route and wired it to the existing dashboard surveys list action plus sidebar context action, keeping the App Router entry file thin.
- Added a dedicated dashboard surveys list view with empty and error states, localized status and visibility chips, survey metadata cards, and permission-based admin CTAs.
- Files changed: `src/app/dashboard/surveys/page.tsx`, `src/modules/surveys/shared/ui/dashboard-surveys-list-view.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Browser verification using the dev-browser skill was not possible because that skill/tooling is unavailable in this Codex session, so manual browser verification is still required.
- **Learnings for future iterations:**
  - Dashboard list surfaces can stay server-rendered and thin by composing existing actions rather than pushing new tenant or permission logic into `src/app`.
  - The survey dashboard needs UI-level permission gating separate from membership-only read access, because members may answer surveys without seeing management CTAs.
  - Linking the list cards to future member, edit, and results routes keeps the survey navigation contract consistent before those pages are implemented.
---

## 2026-04-02 16:33 UTC - US-025
- Refactored `SurveyBuilder` into a single controlled contract that now covers survey title, description, visibility, anonymous-answer toggle, startsAt, endsAt, and the full questions array.
- Added add, remove, and reorder support for both questions and options while renormalizing every position to base-1 after each mutation.
- Rendered an explicit locked state for structural survey fields so future edit flows can disable question and option changes once a survey has responses.
- Files changed: `src/modules/surveys/shared/ui/survey-builder.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Browser verification using the requested dev-browser skill was not possible because that skill/tooling is unavailable in this Codex session, so manual browser verification is still required.
- **Learnings for future iterations:**
  - The shared builder is now shaped for `new` and `edit` pages as one controlled value object, which is simpler than keeping parallel callbacks for each field once the survey contract includes scheduling and metadata.
  - Structural lock behavior should disable the full question editor surface, not only add or remove buttons, because the backend also treats question title, description, type, required flag, and options as locked after the first response.
  - Option and question reordering should always run through a renormalization helper so UI array operations never drift from the database's base-1 ordering contract.
---

## 2026-04-02 16:38 UTC - US-026
- Created the protected `/dashboard/surveys/new` route and kept the App Router entry thin by delegating tenant lookup and authorization to a dedicated surveys context action.
- Added a new survey creation form in the surveys module that reuses `SurveyBuilder`, persists drafts through `createSurveyAction`, shows toast feedback, and redirects back to the dashboard survey list after a successful save.
- Enforced page access for authenticated users with active membership who are either `OWNER` or hold `surveys.manage`, matching the survey creation use-case contract instead of relying on sidebar-only permission data.
- Files changed: `src/app/dashboard/surveys/new/page.tsx`, `src/modules/surveys/server/slices/get-create-survey-context/actions/get-create-survey-context.action.ts`, `src/modules/surveys/server/slices/get-create-survey-context/use-cases/get-create-survey-context.use-case.ts`, `src/modules/surveys/shared/ui/create-survey-form.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Quality gates run: `npm run fix:biome && npm run lint:biome && npm run typecheck`.
- Result: all required gates passed. Biome still reports the pre-existing non-blocking `document.cookie` warning in `src/shared/components/ui/sidebar.tsx`. Browser verification using the requested dev-browser skill was not possible because that skill/tooling is unavailable in this Codex session, so manual browser verification is still required.
- **Learnings for future iterations:**
  - Survey management pages cannot assume sidebar permissions are enough for authorization because `OWNER` access is enforced through membership role in the create/edit use-cases.
  - `SurveyBuilder` already carries the full draft contract, so new management pages should wrap it with route-specific submit behavior instead of introducing a second survey form state shape.
  - Redirecting back to `/dashboard/surveys` after a successful create is the safe default until the dedicated edit/manage route is in place.
---
