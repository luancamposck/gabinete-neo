# Ralph Claude Compatibility Note

`scripts/ralph/ralph.sh` no longer reads this file directly.

Claude runs now use:

- `scripts/ralph/shared-contract.md`
- `scripts/ralph/claude.prompt.md`

If you run Claude manually outside `ralph.sh`, follow both files together.
